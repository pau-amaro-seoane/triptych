I recommend that you have a look into the `tools/` folder but, in general:

- Copy the stellar models you need from the upper folder here
- Modify input 
- Execute `./run.sh`
- If not happy, clean with `./clean.sh`
